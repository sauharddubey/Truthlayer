# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Settings & validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Database
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6

# Auth: Supabase-issued JWTs verified via JWKS (ES256/RS256).
python-jose[cryptography]==3.3.0

# Rate limiting (abuse / cost / DoS protection). In-memory by default; uses
# REDIS_URL automatically when set.
slowapi==0.1.9

# Upload content-type validation via magic bytes (pure-Python, no libmagic).
filetype==1.2.0

# Async jobs
celery==5.4.0
redis==5.2.1

# AI / LLM (OpenAI-compatible)
openai==1.57.4

# Embeddings: default is OpenRouter/OpenAI (via the openai client above).
# For the OPTIONAL local fallback (EMBEDDINGS_PROVIDER=local) install:
#   pip install sentence-transformers
# It is omitted here to keep the image small (it pulls in torch).

# Media ingestion & transcription
# Unpinned floor: YouTube breaks old yt-dlp releases frequently, so always pull a
# recent build. Bump this if extraction starts failing.
yt-dlp>=2025.6.0

# Bundled static ffmpeg fallback so local (non-Docker) setups work without a
# system ffmpeg install. Docker still uses the apt ffmpeg (found first on PATH);
# see app/services/ffmpeg_utils.py for the resolution order.
imageio-ffmpeg>=0.5.1

# Document parsing (RAG ingestion)
pypdf==5.1.0
python-docx==1.1.2

# Evidence retrieval
httpx==0.28.1
tavily-python==0.5.0

# Reports
reportlab==4.2.5
